version: 1
task_definition:
  services:
    redis-populate:
      essential: false
    scrapyd-deploy:
      essential: false
    scrapyd-seed:
      essential: false
    corenlp:
      essential: true
